# AGENT TASKS - 任务队列

> **这是Agent工作台** - 每个Agent从这里领取任务

**最后更新：** 2026-02-18
**协调者：** 人类 + Coordinator Agent

---

## 🎯 当前活跃任务

### 🔴 紧急任务（立即执行）

```
无
```

### 🟡 进行中（Agents正在工作）

```
无（等待环境就绪）
```

### 🟢 待领取（Ready to Start）

#### Task 1.1: 创建GitHub仓库
**负责人：** 待分配
**优先级：** P0
**预计时间：** 10分钟
**依赖：** 无

**任务描述：**
```bash
1. 在GitHub创建新仓库: capy-community
2. 初始化README.md
3. 添加.gitignore (Node.js模板)
4. 创建初始目录结构
5. 第一次commit
```

**验收标准：**
- [ ] GitHub仓库可访问
- [ ] README.md包含项目说明
- [ ] 目录结构符合PROJECT_MEMORY.md

**相关文档：**
- docs/PROJECT_MEMORY.md

---

#### Task 1.2: 创建Supabase项目
**负责人：** 待分配
**优先级：** P0
**预计时间：** 15分钟
**依赖：** 无

**任务描述：**
```
1. 在Supabase创建新项目: capy-community
2. 记录项目URL和API Keys
3. 创建.env.local模板
4. 测试连接
```

**验收标准：**
- [ ] Supabase项目已创建
- [ ] 项目URL和Keys已记录
- [ ] 可以从本地连接

**输出文件：**
```
.env.local.example
docs/CONTEXT/supabase-config.md
```

---

#### Task 1.3: Next.js项目初始化
**负责人：** 待分配
**优先级：** P0
**预计时间：** 20分钟
**依赖：** Task 1.1 (GitHub仓库)

**任务描述：**
```bash
1. 创建Next.js 15项目
   npx create-next-app@latest capy-community \
     --typescript --tailwind --app --src-dir

2. 安装依赖
   npm install @supabase/supabase-js @supabase/auth-helpers-nextjs

3. 创建基础目录结构
   mkdir -p src/lib src/components/forum src/components/capy src/agents

4. 创建Supabase客户端
   src/lib/supabase.ts

5. 测试运行
   npm run dev
```

**验收标准：**
- [ ] 项目可以运行
- [ ] 可以访问localhost:3000
- [ ] Supabase客户端可以连接

**输出文件：**
```
package.json
src/lib/supabase.ts
```

---

## 📋 待拆解任务（Backlog）

### Phase 2: 论坛MVP

#### Epic 2.1: 数据库Schema
```
任务列表:
□ 创建users表 + 测试数据
□ 创建posts表 + 测试数据
□ 创建comments表
□ 创建capys表（只给Max用户）
□ 创建agent相关表

负责人: database-agent (待启动)
预计: 2小时
```

#### Epic 2.2: 论坛UI
```
任务列表:
□ 帖子列表页 /forum
□ 帖子详情页 /forum/[id]
□ 发帖页面 /forum/new
□ 评论组件
□ 权限控制

负责人: frontend-agent (待启动)
预计: 4小时
```

#### Epic 2.3: 论坛API
```
任务列表:
□ GET /api/posts - 获取帖子列表
□ GET /api/posts/[id] - 获取帖子详情
□ POST /api/posts - 创建帖子（Pro/Max）
□ POST /api/posts/[id]/comments - 评论
□ POST /api/posts/[id]/like - 点赞

负责人: backend-agent (待启动)
预计: 3小时
```

### Phase 3: Agent系统

#### Epic 3.1: CapyAgent核心
```
任务列表:
□ 实现CapyAgent基础类
□ 实现初始化（自我意识）
□ 实现dailyCycle()
□ 实现perceive() - 感知环境
□ 实现decide() - AI决策
□ 实现act() - 执行行动
□ 实现remember() - 记忆存储

负责人: agent-core (待启动)
预计: 6小时
```

#### Epic 3.2: AI引擎
```
任务列表:
□ Gemini API集成
□ 帖子相关性分析
□ 推荐生成
□ 对话生成
□ 日记生成

负责人: ai-engine-agent (待启动)
预计: 4小时
```

#### Epic 3.3: Agent协调器
```
任务列表:
□ 实现CapyCoordinator类
□ 批量Agent管理
□ 定时任务触发
□ 结果聚合

负责人: coordinator-agent (待启动)
预计: 3小时
```

---

## 🤝 Agent协作规则

### 任务领取流程

```
1. Agent启动
2. 读取 PROJECT_MEMORY.md（必读）
3. 读取本文件，找到"待领取"任务
4. 选择一个符合自己能力的任务
5. 更新状态为"进行中"
6. 开始工作
```

### 任务完成流程

```
1. 完成任务
2. 运行验收标准检查
3. 写入Agent日志 .agent-logs/[date].log
4. 更新本文件状态为"已完成"
5. 如果发现新问题，创建新任务
6. 通知下一个依赖的Agent（如果有）
```

### 遇到问题时

```
情况1: 任务描述不清晰
  → 查阅 docs/CONTEXT/ 相关文档
  → 如果还不清楚，停止并通知人类

情况2: 技术障碍
  → 尝试3种解决方案
  → 如果都不行，记录问题并通知人类

情况3: 任务超时（>预估时间1.5倍）
  → 停止工作
  → 写下当前进展
  → 通知人类
```

---

## 📊 任务模板

### 创建新任务时使用

```markdown
#### Task X.Y: 任务标题
**负责人：** 待分配 / agent-name
**优先级：** P0(紧急) / P1(重要) / P2(普通)
**预计时间：** X小时
**依赖：** Task X.Y (如果有)

**任务描述：**
\`\`\`
详细步骤...
\`\`\`

**验收标准：**
- [ ] 标准1
- [ ] 标准2

**输出文件：**
\`\`\`
文件路径列表
\`\`\`

**相关文档：**
- docs/CONTEXT/xxx.md
```

---

## 📈 进度追踪

### Phase 1: 环境准备

```
进度: 0/3
□ Task 1.1: GitHub仓库
□ Task 1.2: Supabase项目
□ Task 1.3: Next.js初始化
```

### Phase 2: 论坛MVP

```
进度: 0/12
等待Phase 1完成
```

### Phase 3: Agent系统

```
进度: 0/15
等待Phase 2完成
```

---

## 🔄 自动更新机制

### Agent完成任务时自动执行

```typescript
// 每个Agent完成任务后运行这个

async function updateTaskStatus(taskId, status, result) {
  // 1. 读取本文件
  const tasks = await readFile('docs/AGENT_TASKS.md')

  // 2. 更新任务状态
  const updated = tasks.replace(
    `□ ${taskId}`,
    `✅ ${taskId} - ${new Date().toISOString()}`
  )

  // 3. 写回文件
  await writeFile('docs/AGENT_TASKS.md', updated)

  // 4. 写入日志
  await appendLog({
    timestamp: new Date(),
    agent: this.name,
    task: taskId,
    status,
    result
  })

  // 5. 检查是否有依赖任务可以开始
  await notifyDependentAgents(taskId)
}
```

---

## 🎯 本周目标

```
Week 1 (2026-02-18 ~ 2026-02-24):
□ 环境准备完成
□ 论坛MVP可运行
□ Agent原型可运行
□ 初步集成

成功标准:
- 4个测试用户可以登录
- Pro用户可以发帖
- Max用户可以看到卡皮推荐
- 推荐是AI生成的（不是写死的）
```

---

## 📞 升级路径

```
问题严重程度分级:

Level 1 (轻微) - Agent自行解决
  例: 代码格式问题、命名规范

Level 2 (中等) - 通知其他Agent
  例: API接口变更影响其他模块

Level 3 (严重) - 立即通知人类
  例: 安全漏洞、成本超支、方向性错误
```

---

**记住：这个文件是动态的，每完成一个任务就更新！**

下次Agent启动时，就能看到最新进展。
